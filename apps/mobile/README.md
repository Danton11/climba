# Climba Mobile App (React Native + Expo)

- Uses Apollo Client to talk to AppSync GraphQL API.
- Reads config from environment variables.

## Dev
- `pnpm i`
- `pnpm dev` (from repo root) then start Expo in this folder.
