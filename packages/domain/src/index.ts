// Domain schemas (placeholder). Replace with zod/io-ts schemas for Plan, Session, Diff.
export type PlanDiff = { sessionsAdded?: any[]; sessionsRemoved?: string[]; sessionsModified?: any[]; rationale?: string };
